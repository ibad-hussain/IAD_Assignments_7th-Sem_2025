// Implicit Returns with Array Methods


// Code
const prices = [100, 200, 300];
const withTax = prices.map(price => price * 1.1);
console.log(withTax);   // [110, 220, 330]


// Use Case
    // Arrow functions shine with array methods like map, filter, reduce for concise logic.
